import requests

TRILIUM_URL = "http://localhost:8023"
ETAPI_TOKEN = "your-token-here"  # We don't have it, but we can just ask the user or just construct it locally if there's no auth? Or is ETAPI_TOKEN in .env?
